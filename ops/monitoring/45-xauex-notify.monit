check program xauex-morning-summary with path "/usr/local/lib/monitoring/check_xauex_morning_summary.py --recipient bolyki@bolyki.eu"
  every 1 cycles
  if status != 0 then alert
  group jobs

check program xauex-trade-alerts with path "/usr/local/lib/monitoring/check_xauex_trade_alerts.py --recipient bolyki@bolyki.eu"
  every 1 cycles
  if status != 0 then alert
  group jobs

check program xauex-daily-trade-summary with path "/usr/local/lib/monitoring/check_xauex_daily_trade_summary.py --recipient bolyki@bolyki.eu"
  every 1 cycles
  if status != 0 then alert
  group jobs

check program xauex-signal-stall with path "/usr/local/lib/monitoring/check_xauex_signal_stall.py --recipient bolyki@bolyki.eu"
  every 1 cycles
  if status != 0 then alert
  group jobs
