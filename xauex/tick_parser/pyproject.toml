[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tick_parser"
version = "0.1.0"
description = "High-performance XAUUSD tick data parser — PyO3 Rust extension for XAUEX"
requires-python = ">=3.11"
